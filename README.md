Hopefully this handles requests to the firebase database

Web client -> Server -> Firebase server -> Firebase db

Needs apis for CRUD of origins and drafts

Drafts:

need childs and indexes of the childs it has used
